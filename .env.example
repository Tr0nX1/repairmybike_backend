# Copy this file to `.env` and fill in real values locally.
# Do NOT commit `.env` with secrets. Use environment variables in production
# (Railway/Render/Vercel/etc.) and secret managers where possible.

# --- Core ---
DEBUG=true
SECRET_KEY=replace_in_production
ALLOWED_HOSTS=localhost,127.0.0.1

# Database (optional; SQLite used when not set)
# DATABASE_URL=postgres://user:pass@host:5432/dbname

# --- Media Storage Selection ---
# Enable only ONE storage provider in an environment.
USE_CLOUDINARY=false
USE_CLOUDFLARE_R2=false

# --- Cloudinary (preferred) ---
# Choose ONE of the following auth methods (do not set both):

# Option A: Consolidated URL (simple)
# CLOUDINARY_URL=cloudinary://<api_key>:<api_secret>@<cloud_name>

# Option B: Discrete keys (preferred for secret managers)
# CLOUDINARY_CLOUD_NAME=<cloud_name>
# CLOUDINARY_API_KEY=<api_key>
# CLOUDINARY_API_SECRET=<api_secret>

# Security tips:
# - Set DEBUG=false in production.
# - Rotate API keys periodically; use least-privilege keys.
# - Keep logging at INFO without printing secrets.
# - Never embed secrets in code; rely on env vars.